name: Tests

on:
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Checkout file
      uses: moonrailgun/branch-filestorage-action@v1.2.2
      with:
        path: date
        # 配置安装 python 环境
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.8.2
    # 配置安装依赖包
    - name: Install requirements
      run: pip install -r requirements.txt
    - name: Read and show
      run:
        echo $(date)
        cat date
#         python monthly.py github ${{ secrets.KEY}}

